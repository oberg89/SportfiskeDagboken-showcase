# Frontend showcase

The frontend sample is deliberately compact and uses TypeScript and Vite, the actual language/build foundation used by the product frontend.

It demonstrates domain types, one relative-URL API adapter, a small session form renderer, explicit error rendering, and a testable browser-storage queue. The complete production screen set, visual system, maps, account flows, admin frontend, and proprietary product logic are not published.
